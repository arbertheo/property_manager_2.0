<div>
  <ul>
  <% @properties.each do |property| %>
    <li><%= link_to property.address, property_path(property) %></li>
    <%= link_to  'EDIT', edit_property_path(property), class: 'button' %>
  <% end %>
  </ul>
  <%= link_to 'New property Form', new_property_path, class: 'button'  %>
</div>
